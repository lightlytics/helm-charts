{{- if not .Values.stream_security.apiTokenExistingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "stream_security.fullname" . }}
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
  api-key: {{ default "MISSING" .Values.stream_security.apiToken | b64enc | quote }}
{{- end }}
